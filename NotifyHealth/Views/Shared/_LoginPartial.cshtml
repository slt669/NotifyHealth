@using Microsoft.AspNet.Identity
<!-- begin #header -->
<div id="header" class="header navbar navbar-default navbar-fixed-top">
    <!-- begin container-fluid -->
    <div class="container-fluid">
        <!-- begin mobile sidebar expand / collapse button -->
        @*@if (Request.IsAuthenticated)
        {*@
            <div class="navbar-header">
                <a href="@Url.Action("Index", "Home")" class="navbar-brand"><img src="~/Content/img/logo.png" class="logo" alt="" /> Notify Health</a>
                <button type="button" class="navbar-toggle" data-click="sidebar-toggled">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
            </div>
            <!-- end mobile sidebar expand / collapse button -->
            <!-- begin navbar-right -->
            <ul class="nav navbar-nav navbar-right">
                @if (@Session["timestamp"] != null)
                {

                <li class="dropdown navbar-user">
                    <a href="" class="dropdown-toggle" data-toggle="dropdown">
                        <span style="color:black; padding-right:1px;">Last refreshed: @Session["timestamp"].ToString()</span>
                    </a>
                </li>
                }
                <li class="dropdown navbar-user">
                    @if (@Session["timestamp"] != null)
                    {

                    <a href="javascript:;" class="dropdown-toggle" data-toggle="dropdown">
                        @if (@Session["Photo"] != null)
                        {
                            <span class="image"><img src="@Session["Photo"].ToString()" alt="@Session["UserFullName"].ToString()" /></span>
                        }
                            <span class="hidden-xs">@Session["UserFullName"].ToString()</span> <b class="caret"></b>
                        </a>
                    }
                        <ul class="dropdown-menu pull-right">
                            <li><a href="@Url.Action("Settings", "Account")">Settings</a></li>
                            <li class="divider"></li>
                            <li><a href="@Url.Action("Logoff", "Account")">Log out</a></li>
                        </ul>
                    </li>
            </ul>
            <!-- end navbar-right -->
        @*}*@
        </div>
    <!-- end container-fluid -->
</div>